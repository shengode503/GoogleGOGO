# Complexity: O(n), O(1)
def solve(prices):
    result = 0
    prev_price = prices[0]
    for i in range(1, len(prices)):
        if prices[i] > prev_price:
            result += prices[i] - prev_price
        prev_price = prices[i]
    return result

